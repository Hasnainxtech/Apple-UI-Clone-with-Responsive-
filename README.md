# Apple UI Clone (HTML & CSS only)
This project is a **static UI clone** created using only HTML and CSS for learning purposes. It does **not** accept user credentials, stores no data, and contains no backend.
